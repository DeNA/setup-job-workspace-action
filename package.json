{
  "name": "setup-job-workspacce-action",
  "version": "0.0.0",
  "private": true,
  "description": "A action create workspace to checkout repository for each jobs",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc",
    "package": "ncc build --source-map --license licenses.txt",
    "lint": "ts-standard src/**/* __tests__/**/*",
    "lint:fix": "ts-standard --fix src/**/* __tests__/**/*",
    "test": "jest",
    "all": "npm run build && npm run lint && npm run package && npm test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kesin11-private/setup-job-workspace-action.git"
  },
  "keywords": [
    "actions",
    "node",
    "setup"
  ],
  "author": "kesin1202000@gmail.com",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0"
  },
  "devDependencies": {
    "@types/node": "^18.11.2",
    "@vercel/ncc": "^0.34.0",
    "jest": "^29.2.0",
    "js-yaml": "^4.1.0",
    "ts-jest": "^29.0.3",
    "ts-standard": "^12.0.1",
    "typescript": "^4.8.4"
  }
}
